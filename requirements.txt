# Core server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0

# HTTP client for upstream requests
httpx[http2]==0.25.2
h2>=4.0.0
httpcore[http2]>=1.0.0
aiohttp>=3.9.0

# Data validation and settings (Python 3.8 compatible)
pydantic>=1.10.0,<2.0.0
python-dotenv==1.0.0

# OpenAI SDK for testing and examples
openai>=1.0.0
